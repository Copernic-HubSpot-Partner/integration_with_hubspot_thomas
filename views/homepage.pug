doctype html
html 
    head 
        title= title
        meta(name="viewport" content="width=device-width, initial-scale=1")
        link(rel="stylesheet", href="/css/style.css")
    body    
        h1 Custom Objects
        a(href="/update-cobj") Ajouter un nouvel objet
        .cards 
            each obj in data
                .card 
                    h2.card__name #{obj.properties.nom}
                    p.card__description #{obj.properties.description}
                    p.card__category #{obj.properties.attaque}